# Currency Watchlist Manager

This program is a simple currency watchlist manager. It was built using sets. It prints out the active sessions that aren't in the archive sessions, the currencies in both lists,  and every currency pair.

## What I learnt:
1. How to use the different operators with sets such as the difference, intersection, and union operator.
